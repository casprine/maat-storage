# Event: ticket.commented

| Field | Value |
|---|---|
| Event ID | E-20260801-195513-casprine-9be4 |
| Time | 2026-08-01T19:55:13+02:00 |
| Actor | casprine |
| Project | driver |
| Type | ticket.commented |
| Object | T-20260801-193502-5676 |

## Summary

Reviewer packet for Lorentz

Changed files:
- apps/api/internal/config/config.go, config_test.go
- apps/api/internal/domain/job.go, search.go
- apps/api/internal/http/httpx/embedding.go, jobs.go, library.go, router.go, search_embedding_test.go
- apps/api/internal/jobs/requests.go, service.go
- apps/api/internal/postgres/job_complete.go, job_index.go, search.go
- apps/api/internal/database/migrations/0002-embedding-metadata.sql
- apps/api/openapi.json
- apps/worker/internal/orchestrate/api_job_runner.go, api_jobs.go
- apps/worker/internal/processing/embedding.go, embedding_test.go, pipeline_embeddings.go, pipeline_models.go, processing.go, pipeline_steps.go
- deploy/.env.example, deploy/docker-compose.yml, deploy/docker-compose.dev.yml
- docs/configuration-plan.md

Implementation summary:
- Adds DRIVE_EMBEDDING_MODE disabled/external/local with model, dimensions, optional version/hash.
- Keeps DRIVE_EMBEDDING_URL provider boundary; local mode defaults to TEI OpenAI-compatible /v1/embeddings endpoint.
- Adds worker dimension checks before applying embeddings.
- Adds worker completion embeddingMetadata and API-side sanitization so disabled/mismatched providers store text without vectors.
- Adds pgvector migration from fixed vector(1536) to vector plus provider/model/dimension/version/stale columns.
- Search filters semantic vectors by current provider/model/dimension/version and vector_dims before pgvector comparison.
- Completion marks existing mismatched vectors stale when an enabled embedding contract completes.
- Adds profile-gated embedding sidecar in Compose; credentials remain optional.

Threat/risk notes:
- Migration changes embedding columns to unconstrained vector; app-level dimension and search filters become the safety boundary.
- Existing 1536-dim rows without metadata will not be used for semantic search after an embedding provider is configured until reprocessed.
- Stale marking happens when jobs complete under an enabled embedding contract; changing env alone safely excludes old vectors from semantic search but does not proactively enqueue reprocessing.
- API completion accepts legacy workers without metadata only if dimensions match configured API metadata.
- Compose sidecar uses Hugging Face TEI CPU image and no healthcheck, to avoid helper health scope.

Validation:
- env GOCACHE=/private/tmp/drive-local-embedding-go-cache make test-fast (passed, escalated for httptest localhost binds)
- make check-code-lines (passed)
- make check-markdown-lines (passed with existing docs aggregate warning)
- make test-openapi-smoke (passed)
- docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config --quiet with DRIVE_ENV_FILE=.env.example (passed)
- docker compose -f deploy/docker-compose.dev.yml config --quiet (passed)
- rg conflict markers in touched areas (none)
- git diff --check (passed)

Known dependencies:
- Work was done in isolated clone /private/tmp/drive-local-embedding to avoid unrelated unstaged edits in the shared worktree.
- apps/web dependencies were installed locally so Orval SDK generation could run; generated SDK remains gitignored.
