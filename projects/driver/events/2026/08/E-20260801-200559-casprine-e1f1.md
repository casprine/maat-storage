# Event: ticket.commented

| Field | Value |
|---|---|
| Event ID | E-20260801-200559-casprine-e1f1 |
| Time | 2026-08-01T20:05:59+02:00 |
| Actor | casprine |
| Project | driver |
| Type | ticket.commented |
| Object | T-20260801-193502-5676 |

## Summary

Updated reviewer packet for Lorentz after blocked review

Resolved review findings:
- P1 TEI host exposure: removed the embedding service host port from deploy/docker-compose.yml and deploy/docker-compose.dev.yml; removed DRIVE_EMBEDDING_PORT from deploy/.env.example. The TEI sidecar is now service-network only by default.
- P1 local startup/no-vector indexes: local embedding mode now sets worker embeddingRequired=true. Provider failures, count mismatches, missing required provider, and dimension mismatches now fail processing instead of returning a partial embeddings step, so api_job_runner fails the job and normal job retry logic applies. External embeddings remain optional and still fall back to text-only indexing.
- P2 model-version overlap: docs/configuration-plan.md now documents that chunk embedding_* columns are denormalized search-contract copies only. If T-20260801-193538-8f76 lands a processing-output version table, that table is the source of truth for model version history/reprocess planning, and this branch should populate chunk columns from it during rebase.

Changed since prior packet:
- deploy/docker-compose.yml
- deploy/docker-compose.dev.yml
- deploy/.env.example
- apps/worker/internal/processing/processing.go
- apps/worker/internal/processing/embedding.go
- apps/worker/internal/processing/pipeline_embeddings.go
- apps/worker/internal/processing/pipeline_models.go
- apps/worker/internal/processing/embedding_test.go
- docs/configuration-plan.md

Validation after fixes:
- env GOCACHE=/private/tmp/drive-local-embedding-go-cache make test-fast (passed, escalated for httptest localhost binds)
- make check-code-lines (passed)
- make check-markdown-lines (passed with existing docs aggregate warning)
- make test-openapi-smoke (passed)
- env DRIVE_ENV_FILE=.env.example docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config --quiet (passed)
- docker compose -f deploy/docker-compose.dev.yml config --quiet (passed)
- git diff --check (passed)
- conflict-marker search in touched areas (no matches)

Approval status:
- Not committed, pushed, or PR opened. Waiting on Lorentz approval per ticket instruction.
